[project]
name = "ctl"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
authors = [
    { name = "Tactor", email = "119103845+tactordev@users.noreply.github.com" }
]
requires-python = ">=3.13"
dependencies = ["pdflatex", "jinja2"]

[project.scripts]
ctl = "ctl:main"

[build-system]
requires = ["uv_build>=0.11.28,<0.12.0"]
build-backend = "uv_build"
